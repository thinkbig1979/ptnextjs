# ======================
# Development Environment
# ======================

# Node environment (development | production | test)
NODE_ENV=development

# Next.js Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# ======================
# Payload CMS Configuration
# ======================

# Database Configuration
# SQLite for development (zero-configuration)
DATABASE_URL=file:./data/payload.db

# Payload CMS Secret Key
# ⚠️ SECURITY CRITICAL: This is REQUIRED - app will not start without it
# Generate a secure secret: openssl rand -base64 32
# Minimum length: 32 characters
# NEVER commit actual secrets to version control!
PAYLOAD_SECRET=REPLACE_ME_WITH_SECURE_SECRET_MINIMUM_32_CHARACTERS

# Payload CMS Server URL
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# JWT Configuration
JWT_ACCESS_TOKEN_EXPIRY=1h
JWT_REFRESH_TOKEN_EXPIRY=7d

# Admin Configuration (change immediately after first login)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin123!@#SecurePassword

# ======================
# TinaCMS Configuration (Legacy - being phased out)
# ======================
TINA_CLIENT_ID=your_tina_client_id_here
TINA_TOKEN=your_tina_token_here

# ======================
# Production Environment
# ======================
# Uncomment and configure these for production deployment:

# PostgreSQL database URL (production)
# DATABASE_URL=postgresql://user:password@host:5432/database

# Production server URL
# NEXT_PUBLIC_SERVER_URL=https://your-production-domain.com

# Production secret (generate new one for production)
# PAYLOAD_SECRET=production-secret-key-minimum-32-characters

# Build Configuration
# NOTE: NEXT_OUTPUT_MODE=export is incompatible with Payload CMS
# Remove this line or comment it out when using Payload CMS
# NEXT_OUTPUT_MODE=export

# ======================
# Migration Configuration
# ======================
MIGRATION_DRY_RUN=false
MIGRATION_BACKUP_DIR=./backups/migration
MIGRATION_LOG_LEVEL=info

# ======================
# hCaptcha Configuration
# ======================
# Get your keys from: https://dashboard.hcaptcha.com/
# Site key (public, exposed to client)
NEXT_PUBLIC_HCAPTCHA_SITE_KEY=your-hcaptcha-site-key-here
# Secret key (private, server-side only)
HCAPTCHA_SECRET_KEY=your-hcaptcha-secret-key-here

# ======================
# Email Configuration (Resend)
# ======================
# Get your API key from: https://resend.com/api-keys
# Note: Verify your domain in Resend dashboard before sending emails
RESEND_API_KEY=re_your_api_key_here
# Email address for sending notifications (must be from verified domain)
EMAIL_FROM_ADDRESS=notifications@yourdomain.com
# Admin email address for receiving notifications (new vendor registrations, tier requests)
ADMIN_EMAIL_ADDRESS=admin@yourdomain.com
# Base URL for email links (dashboard, admin panel)
NEXT_PUBLIC_BASE_URL=http://localhost:3000