// Payload CMS Configuration
// Re-export the configuration from payload.config.ts
// This ensures compatibility with Payload CMS import expectations

import config from './payload.config';

export default config;
